<!DOCTYPE html>
<html lang="th">
<head>
  <meta charset="UTF-8">
  <title><%= product.name %> | MobileGear</title>
  <style>
    body {
      font-family: 'Prompt', sans-serif;
      background-color: #f8f9fa;
      margin: 0;
    }

    header {
      background: #1B3C73;
      color: #FFD600;
      padding: 15px 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    header a {
      color: #FFD600;
      text-decoration: none;
      font-weight: bold;
    }

    .container {
      display: flex;
      gap: 40px;
      width: 80%;
      margin: 40px auto;
      background: #fff;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .image-section img {
      width: 350px;
      height: 350px;
      object-fit: cover;
      border-radius: 10px;
    }

    .details {
      flex: 1;
    }

    .details h2 {
      margin: 0 0 15px;
      color: #1B3C73;
    }

    .price {
      font-size: 22px;
      color: #E53935;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .stock {
      color: green;
      margin-bottom: 20px;
    }

    .qty-box {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 20px;
    }

    .qty-box button {
      width: 30px;
      height: 30px;
      border: none;
      background: #1B3C73;
      color: #FFD600;
      font-size: 18px;
      cursor: pointer;
      border-radius: 5px;
    }

    .qty-box input {
      width: 50px;
      text-align: center;
      padding: 5px;
    }

    .btn-add {
      background: #FFD600;
      color: #1B3C73;
      border: none;
      padding: 12px 20px;
      border-radius: 8px;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.2s;
    }
    .btn-add:hover {
      background: #1B3C73;
      color: #fff;
    }

    .desc {
      margin-top: 20px;
      color: #555;
    }
  </style>
</head>
<body>
  <header>
    <h2>📱 MobileGear</h2>
    <a href="/products">← กลับหน้าสินค้า</a>
  </header>

  <div class="container">
    <div class="image-section">
      <img src="<%= product.img %>" alt="<%= product.name %>">
    </div>

    <div class="details">
      <h2><%= product.name %></h2>
      <div class="price">฿<%= product.price %></div>
      <div class="stock">คงเหลือ: <%= product.stock %> ชิ้น</div>

      <div class="qty-box">
        <button type="button" onclick="updateQty(-1)">-</button>
        <input type="text" id="qty" value="1" readonly>
        <button type="button" onclick="updateQty(1)">+</button>
      </div>

      <!-- ✅ ส่ง slug + qty -> /cart/add -->
      <form action="/cart/add" method="POST">
        <input type="hidden" name="slug" value="<%= product.slug %>">
        <input type="hidden" id="hiddenQty" name="qty" value="1">
        <button class="btn-add" type="submit">เพิ่มลงตะกร้า</button>
      </form>

      <div class="desc">
        <%= product.desc %>
      </div>
    </div>
  </div>

  <script>
    function updateQty(change) {
      const qtyInput = document.getElementById('qty');
      let current = parseInt(qtyInput.value) || 1;
      current += change;
      if (current < 1) current = 1;
      qtyInput.value = current;
      document.getElementById('hiddenQty').value = current;
    }
  </script>
</body>
</html>
